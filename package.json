{
  "private": true,
  "scripts": {
    "start": "npm install && gulp",
    "build": "npm install && gulp build"
  },
  "devDependencies": {
    "typescript": "latest",
    "gulp": "latest",
    "gulp-typescript": "latest",
    "gulp-uglify": "latest",
    "gulp-open": "latest"
  }
}